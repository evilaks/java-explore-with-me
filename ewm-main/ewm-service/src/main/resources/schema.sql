CREATE TABLE IF NOT EXISTS "events" (
                          "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                          "annotation" varchar(255),
                          "category_id" int,
                          "confirmed_requests" int,
                          "created_on" timestamp,
                          "description" varchar(255),
                          "event_date" timestamp,
                          "initiator_id" int,
                          "location_id" int,
                          "paid" boolean,
                          "participant_limit" int,
                          "published_on" timestamp,
                          "request_moderation" boolean,
                          "state_id" int,
                          "title" varchar(64),
                          "views" int
);

CREATE TABLE IF NOT EXISTS "users" (
                         "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                         "name" varchar(255),
                         "email" varchar(255),
                         "is_approved" boolean
);

CREATE TABLE IF NOT EXISTS "locations" (
                             "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                             "lat" float,
                             "lon" float
);

CREATE TABLE IF NOT EXISTS "categories" (
                              "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                              "name" varchar(64)
);

CREATE TABLE IF NOT EXISTS "compilations" (
                                "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                                "title" varchar(64),
                                "pinned" boolean
);

CREATE TABLE IF NOT EXISTS "compilations_events" (
                                       "compilation_id" int,
                                       "event_id" int,
                                       PRIMARY KEY ("compilation_id", "event_id")
);

CREATE TABLE IF NOT EXISTS "participation_requests" (
                                          "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                                          "created" timestamp,
                                          "event_id" int,
                                          "requester" int,
                                          "status" int
);

CREATE TABLE IF NOT EXISTS "event_states" (
                                "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                                "title" varchar(16)
);

CREATE TABLE IF NOT EXISTS "request_statuses" (
                                    "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                                    "title" varchar(16)
);

ALTER TABLE "participation_requests" ADD FOREIGN KEY ("event_id") REFERENCES "events" ("id");

ALTER TABLE "events" ADD FOREIGN KEY ("state_id") REFERENCES "event_states" ("id");

ALTER TABLE "events" ADD FOREIGN KEY ("initiator_id") REFERENCES "users" ("id");

ALTER TABLE "events" ADD FOREIGN KEY ("location_id") REFERENCES "locations" ("id");

ALTER TABLE "events" ADD FOREIGN KEY ("category_id") REFERENCES "categories" ("id");

ALTER TABLE "compilations_events" ADD FOREIGN KEY ("compilation_id") REFERENCES "compilations" ("id");

ALTER TABLE "compilations_events" ADD FOREIGN KEY ("event_id") REFERENCES "events" ("id");

ALTER TABLE "participation_requests" ADD FOREIGN KEY ("requester") REFERENCES "users" ("id");

ALTER TABLE "participation_requests" ADD FOREIGN KEY ("status") REFERENCES "request_statuses" ("id");
